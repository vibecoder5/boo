---
description: Суть boo, инварианты и куда смотреть
alwaysApply: true
---

# boo

Офлайн-читалка: Go + встроенный `web/`. Данные только локально, HTTP только с 127.0.0.1. UI и ошибки — по-русски.

- Старт и флаги: `main.go` (`-port`, `-web`, `-no-open`, `-demo`).
- Форматы → `*epub.Book`: `internal/open`, парсеры `epub` / `fb2` / `txt`.
- API и UI: `internal/server`, статика `web/`.
- Persist: `internal/store` → `%AppData%/boo` (`state.json`, `library/`, `covers/`, `dictionaries/`).
- Словари: `internal/dict`. Окно: `internal/desktop` (Windows/WebView2).

Инварианты: не добавлять сеть и аккаунты; HTML глав санитайзить; `/res` — только картинки и шрифты; импорт ZIP заменяет библиотеку; undo покрывает книгу / закладку / заметку, не highlight.

Документация: `README.md`, `docs/user-guide.md`, `CHANGELOG.md`, `AGENTS.md`. Версии — SemVer в `CHANGELOG.md`. После правок store/парсеров/API — `go test ./...`.
